import React from "react";
import FiltersList from "../FilterVideos/FiltersList";

const MainContainer = () => {
  return (
    <div className="ml-24 mr-5 pt-16">
      <FiltersList />
    </div>
  );
};

export default MainContainer;
